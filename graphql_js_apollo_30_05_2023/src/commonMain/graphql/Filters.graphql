query Themes($page: Int!, $pageSize: Int!){
    getAllThemes(page:$page, pageSize:$pageSize)
}

query Frequencies($page: Int!, $pageSize: Int!){
    getAllFrequencies(page:$page, pageSize:$pageSize)
}

query Publishers($page: Int!, $pageSize: Int!){
    getAllPublishers(page:$page, pageSize:$pageSize)
}

query AdminLevels($page: Int!, $pageSize: Int!){
    getAllAdminLabel(page:$page, pageSize:$pageSize)
}
query Keywords($page: Int!, $pageSize: Int!){
    getAllKeywords(page:$page, pageSize:$pageSize)
}

query Formats($page: Int!, $pageSize: Int!) {
    getAllFormats(page:$page, pageSize:$pageSize)
}